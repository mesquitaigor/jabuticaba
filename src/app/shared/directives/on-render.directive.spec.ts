import { OnRenderDirective } from './on-render.directive';

describe(OnRenderDirective.name, () => {
  it('should create an instance', () => {
    const directive = new OnRenderDirective();
    expect(directive).toBeTruthy();
  });
});
